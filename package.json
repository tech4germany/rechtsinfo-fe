{
  "name": "rechtsinfo-fe",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "sass": "sass --style=compressed src/scss/styles.scss src/_includes/css/styles.css",
    "watch:eleventy": "eleventy cross-env NODE_ENV=development --serve",
    "watch:sass": "npm run sass -- --watch",
    "start": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && eleventy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^0.10.0",
    "elasticlunr": "^0.9.5",
    "govuk-frontend": "^3.8.1",
    "npm-run-all": "^4.1.5",
    "require-dir": "^1.2.0",
    "sass": "^1.26.10"
  },
  "devDependencies": {
    "node-fetch": "^2.6.0",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2"
  }
}
